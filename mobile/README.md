# MoneyApp mobile
React Native 0.87 + TypeScript. `npm install`, then `npx react-native run-android` / `run-ios` (iOS: `bundle install && cd ios && bundle exec pod install`).
API base URL: `src/config.ts`. Add a language: `src/i18n/locales/<code>.ts` + one entry in `src/i18n/index.ts`.
